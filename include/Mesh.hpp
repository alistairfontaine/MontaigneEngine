#ifndef MESH_HPP
#define MESH_HPP

#include <vector>

struct Mesh {
    unsigned int vao;
    unsigned int vbo;
    int vertexCount; // Your code was looking for 'count', we keep it matching here
};

#endif // MESH_HPP
